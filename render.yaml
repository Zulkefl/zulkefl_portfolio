services:
  - type: web
    name: zulkefl-portfolio
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GMAIL_USER
        value: zulkeflrehman@gmail.com
      - key: GMAIL_PASSWORD
        sync: false
      - key: PORT
        value: 3000
